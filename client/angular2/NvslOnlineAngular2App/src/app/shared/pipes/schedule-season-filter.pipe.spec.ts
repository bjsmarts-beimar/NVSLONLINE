import { ScheduleSeasonFilterPipe } from './schedule-season-filter.pipe';

describe('ScheduleSeasonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleSeasonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
