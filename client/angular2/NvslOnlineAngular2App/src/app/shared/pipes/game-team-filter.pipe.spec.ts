import { GameTeamFilterPipe } from './game-team-filter.pipe';

describe('GameTeamFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameTeamFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
