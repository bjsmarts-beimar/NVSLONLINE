import { DivisionScheduleFilterPipe } from './division-schedule-filter.pipe';

describe('DivisionScheduleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DivisionScheduleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
