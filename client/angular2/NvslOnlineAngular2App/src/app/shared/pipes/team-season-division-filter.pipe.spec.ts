import { TeamSeasonDivisionFilterPipe } from './team-season-division-filter.pipe';

describe('TeamSeasonDivisionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamSeasonDivisionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
