import { ScheduleSeasonDivisionPipe } from './schedule-season-division.pipe';

describe('ScheduleSeasonDivisionPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleSeasonDivisionPipe();
    expect(pipe).toBeTruthy();
  });
});
