import { DivisionFilterPipe } from './division-filter.pipe';

describe('DivisionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DivisionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
